Now checks for the unkown error in the list pairings functions
Added an actual GUI interface with buttons, not just a toolbar
Sped up startup by maximising the use of windows api calls instead of ported mingw ones
Now uses cleaner way of extracting backend
Now tells you if there was an error while extracting the backend
Now uses a higher resolution icon, increasing the size of the app drastically
