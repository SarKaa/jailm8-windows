Now doesn't freeze up when apple mobile device 64 isn't installed (needs latest version of backend)
Improved memory usage drastically
Now shows output in actual window, instead of a message box
Resized buttons/button spacing
Removed documentation button
Now performs the "stats" function at startup 
Improved downloading indicators, now shows it in the window and automatically updates status
Might take some extra time to startup
