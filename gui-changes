Terminates properly if saved as jailm8.exe
Replaces checkra1n button with sideserver setup button
Added jitterbugpair tools 
Added idevicesetlocation and idevicedevmodectl integration
Fixes issues with the new libimobiledevice breaking some functions
Fixed pairing list function (requires latest limd)
Can now install itunes/apple software update
Any opened CLI windows will no longer freeze the rest of the app
