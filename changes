CHANGELOG:
Now checks if Apple Mobile Device Support 64 is installed when running libimobiledevice files. 
If it isn't installed, jailm8 will ask if you want to install it, if you say yes, it will download the installer and start it.