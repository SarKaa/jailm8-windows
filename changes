CHANGELOG:
Tried to fix the error in the version command that prints an empty "iOS version:" string even when no device is connected. (this still happens, idk why)
Fixed bugging out when running info command without a connected device